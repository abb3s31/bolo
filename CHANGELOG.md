# سجل التغييرات الأكاديمي (Academic Changelog) 📜✨

جميع التحديثات البرمجية والتحسينات المعمارية لنظام مسار بولونيا الأكاديمي - جامعة الإمام جعفر الصادق (ع) - فرع ميسان.

---

## [الإصدار 1.8.0] - 2026-09-11 🚀

### 🌟 الميزات والإضافات الجديدة (Added):
- **نظام عزل الكروبات المستقلة 100%:**
  - تمكين كل كروب من امتلاك جدول محاضرات خاص به ومستقل تماماً دون تداخل مع الكروبات الأخرى.
  - إضافة دعم تكوينات الكروبات حسب المرحلة الأكاديمية ونوع الدوام (صباحي / مسائي) عبر خدمة `groups-service.ts`.
  - إظهار خيارات الكروبات حصراً في المراحل المقسمة لكروبات، وإظهار شارة `لا يوجد كروبات لهذه المرحلة (شعبة موحدة)` للمراحل غير المقسمة.
  - إزالة وإلغاء كلمة "مشتركة" نهائياً من كافة الجداول والواجهات الأكاديمية واستبدالها بمصطلح "شعبة موحدة".
- **نافذة المحاضرات (`LectureModal.tsx`):**
  - فحص تكوين كروبات المرحلة ونوع الدوام وعرض أزرار الكروبات المعتمدة باللون الكحلي الملكي (`#0F2942`).
  - تعيين الكروب الافتراضي للمحاضرة تلقائياً بدون السماح باختيارات غير متوافقة.
- **تبويب جدول القسم الأكاديمي (`DepartmentScheduleTab.tsx`):**
  - شريط تصفية مستقل بأزرار مخصصة لكل كروب (`[جدول كروب A]`، `[جدول كروب B]`) مع عداد دقيق لعدد محاضرات الكروب.
  - عزل المحاضرات برمجياً بحيث يرى المشرف جدول الكروب المستقل أو الجدول الشامل للقسم.
- **مخطط جدول الطالب والتدريسي (`StudentScheduleTimeline.tsx` & `TeacherScheduleTimeline.tsx`):**
  - عزل صارم لمحاضرات الطالب حسب كروبه المعتمد ودراسته (صباحي / مسائي).
  - إظهار شارة الكروب في كروت المخطط الزمني للتدريسي وفي عرض الأسبوع والطباعة وتصدير Excel.
- **تحديثات قوالب واستيراد Excel (`excel-utils.ts` & `useDepartmentSchedule.ts`):**
  - إضافة عمود الكروب الأكاديمي مع أمثلة تفصيلية وإرشادات استيراد في القالب المعتمد.
  - استيراد وتعيين `target_group` تلقائياً مع معالجة ذكية تمنع تضارب مواعيد الكروبات المختلفة في قاعات منفصلة.
- **تكامل سحابة Supabase ومخطط البيانات (`groups_and_cohorts_schema.sql`):**
  - إضافة جدول `department_stage_groups` مع تفعيل كامل لسياسات الأمان RLS.
  - إضافة حقلي `student_group` و `subgroup` لجدول `students` وفهارس البحث.
  - إضافة حقل `target_group` لجدول `schedule_lectures` وفهرسه المخصص.
  - تحديث عرض `vw_all_profiles` وقادح التوجيه `fn_profiles_view_router` لدعم حفظ واسترجاع الكروب والمختبر.
  - تحديث `supabase-client.ts` و `SupabaseStudentRepository.ts` لحفظ وتزامن واسترجاع الكروبات والبيانات السحابية التراجعية.

### 🛡️ التحسينات ومراقبة الجودة (Quality Assurance):
- اجتياز فحص التايب سكربت `npx tsc --noEmit` بنسبة نجاح 100% وبكود خروج `Exit Code: 0`.
- التزام صارم بقاعدة Type Safety وخلو الكود من أي استخدام لـ `any` أو `unknown`.
- توثيق عراقي كامل لكل سطر برمجي جديد أو معدل.
